# Токен Telegram-бота (обязательно). Получить: @BotFather
BOT_TOKEN=

# Ключ шифрования паролей календарей (обязательно, 32 байта base64)
# Сгенерировать: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SECRET_KEY=

# Telegram ID администраторов бота через запятую (админ-панель, команды /admin и /create)
ADMIN_IDS=

# DSN базы данных (по умолчанию SQLite в корне проекта).
# При запуске в docker-compose этот параметр переопределяется на sqlite+aiosqlite:////data/bot.db
DATABASE_URL=sqlite+aiosqlite:///bot.db

# Интервал опроса Codeforces API, секунды
PARSE_INTERVAL_SECONDS=600

# Интервал проверки напоминаний, секунды
REMIND_INTERVAL_SECONDS=10

# Разрешить пользователям подключать CalDAV-серверы по локальным адресам
# (localhost, 127.0.0.1, 192.168.x.x и т.п.). По умолчанию запрещено.
ALLOW_LOCAL_CALDAV=false

# Прокси для исходящих запросов к Telegram API и Codeforces API.
# Форматы: http://user:pass@host:port, socks4://..., socks5://..., socks5h://...
# (socks5h — DNS резолвится на стороне прокси, рекомендуется).
# ВАЖНО (Docker): прокси, запущенный на хост-машине, доступен не по localhost,
# а по host.docker.internal (отображение уже настроено в docker-compose.yml).
PROXY_URL=

# Прокси только для CalDAV-серверов (пароли календарей идут через него).
# Если пусто — CalDAV работает напрямую. Удобно оставить пустым, если
# CalDAV-сервер доступен напрямую (например, в локальной сети при
# ALLOW_LOCAL_CALDAV=true), а прокси нужен только для Telegram/Codeforces.
CALDAV_PROXY_URL=
